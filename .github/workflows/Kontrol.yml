name: Step 5 Çalıştır (Paralel)

on:
  workflow_dispatch:  # Manuel olarak tetiklenmesi için

jobs:
  run-scripts:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
    steps:
      - name: Checkout public repo (main)
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          ref: main

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Clone Private Py Repo
        run: git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/opitiopiti/Py.git private_py

      - name: Install Shared Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 lxml playwright aiohttp aiofiles dotenv httpx
          playwright install

      - name: Run Scripts in Controlled Parallel Batches
        run: |
          set +e
          
          echo "🔄 Batch 1 başlıyor (HDFilmSite & AnimeHeaven & AnimeNoSub & SetFilmİzle & Diziyo)"
          # ( python private_py/series/hdfilmsite/yenibolumler.py || true ) &
          # ( python private_py/anime/ingilizce/animeheaven/yenibolumler.py || true ) &
          ( python private_py/anime/ingilizce/animenosub/yenibolumler.py || true ) &
          ( python private_py/series/setfilmizledizi/yenibolumler.py || true ) &
          ( python private_py/series/diziyo/yenibolumler.py || true ) &
          wait
          
          echo "🔄 Batch 2 başlıyor (Dizigom & Dizipall & SezonlukDizi & YabancıDiziler)"
          ( python private_py/series/dizigom/yenibolumler.py || true ) &
          ( python private_py/series/dizipall/yenibolumler.py || true ) &
          ( python private_py/series/sezonlukdizi/yenibolumler.py || true ) &
          ( python private_py/series/yabancidiziler/yenibolumler.py || true ) &
          # ( python private_py/series/dizipal/yenibolumler.py || true ) &
          wait
          
          echo "🔄 Batch 3 başlıyor (HDFilmCehennemi & Dizilife)"
          
          ( python private_py/series/hdfilmcehennemi/yenibolumler.py || true ) &          
          # ( python private_py/series/dizilife/yenibolumler.py || true ) &
          # ( python private_py/anime/anizm/yenibolumler.py || true ) & # Kod hatası mevcut şuanda çözmeye gerek yok
          
          wait
          
          echo "✅ Tüm scriptler tamamlandı.."

      - name: Clone the hidden repo (sistemkontrolm3u)
        run: |
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/opitiopiti/sistemkontrolm3u.git
          cd sistemkontrolm3u

      - name: Sync outputs to systemkontrolm3u
        run: |
          # Dizigom
          [ -d "sistemkontrolm3u/dizigom" ] && rsync -a dizigom/ sistemkontrolm3u/dizigom/
          
          # Anizm
          [ -d "sistemkontrolm3u/anizm" ] && rsync -a anizm/ sistemkontrolm3u/anizm/
          
          # AnimeNoSub
          [ -d "sistemkontrolm3u/animenosub" ] && rsync -a animenosub/ sistemkontrolm3u/animenosub/
          
          # SetFilmİzleDizi
          [ -d "sistemkontrolm3u/setfilmizledizi" ] && rsync -a setfilmizledizi/ sistemkontrolm3u/setfilmizledizi/
          
          # DiziYo
          [ -d "sistemkontrolm3u/diziyo" ] && rsync -a diziyo/ sistemkontrolm3u/diziyo/
          
          # DiziPall
          [ -d "sistemkontrolm3u/dizipall" ] && rsync -a dizipall/ sistemkontrolm3u/dizipall/
          
          # HDFilmsite
          [ -d "sistemkontrolm3u/yereldiziler" ] && rsync -a yereldiziler/ sistemkontrolm3u/yereldiziler/
          
          # HDFilmCehennemi
          [ -d "sistemkontrolm3u/hdfilmcehennemi" ] && rsync -a hdfilmcehennemi/ sistemkontrolm3u/hdfilmcehennemi/
          
          # YabancıDiziler
          [ -d "sistemkontrolm3u/yabancidiziler" ] && rsync -a yabancidiziler/ sistemkontrolm3u/yabancidiziler/
          
          # SezonlukDİzi
          [ -d "sistemkontrolm3u/sdizi" ] && rsync -a sdizi/ sistemkontrolm3u/sdizi/
          
          # Dizipal
          [ -d "sistemkontrolm3u/dizipal" ] && rsync -a dizipal/ sistemkontrolm3u/dizipal/
          
          # AnimeHeaven
          [ -d "sistemkontrolm3u/animeheaven" ] && rsync -a animeheaven/ sistemkontrolm3u/animeheaven/
          
          # Dizilife
          [ -d "sistemkontrolm3u/dizilife" ] && rsync -a dizilife/ sistemkontrolm3u/dizilife/

      - name: Commit and Push Changes to Repo
        run: |
          cd sistemkontrolm3u
          
          # Git add all changes
          git add .
          
          # Commit the changes
          git commit -m "Sync updated outputs from scripts" || echo "No changes to commit"
          
          # Push the changes
          git push https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/opitiopiti/sistemkontrolm3u.git
